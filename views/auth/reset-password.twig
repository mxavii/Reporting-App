<!DOCTYPE html>
<html lang="en">

<head>
    <title>Reset Password</title><meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="{{ base_url() }}/css/bootstrap.min.css" />
    <link rel="stylesheet" href="{{ base_url() }}/css/bootstrap-responsive.min.css" />
    <link rel="stylesheet" href="{{ base_url() }}/css/matrix-login.css" />
    <link href="{{ base_url() }}/font-awesome/css/font-awesome.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/admin-lte/2.3.11/css/AdminLTE.min.css">
    <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,700,800'
    rel='stylesheet' type='text/css'>

</head>
<body>
    <div id="loginbox">
        {% include 'templates/partials/flash.twig' %}
        <form id="loginform" class="form-vertical" action="{{path_for('post.reset')}}"
        method="post">
        <div class="control-group normal_text">
            <h3><img src="{{ base_url() }}/assets/images/logo.jpg" alt="Reporting App" /></h3>
        </div>
        <p class="normal_text">Silakan masukkan email akun Reporting App Anda. <br />
            Lalu masukkan password baru.</p>
        <div class="control-group {{ errors.email ? 'has-error' : '' }}">
            <div class="controls">
                <div class="main_input_box">
                    <span class="add-on bg_lo"><i class="icon-envelope"></i></span>
                    <input name="email" type="email"
                    value="{{old.email}}" placeholder="Email" />
                    {% if errors.email %}
                    <span style="color:red;" class="help-inline">{{errors.email | first}}</span>
                    {% endif %}
                </div>
            </div>
        </div>
        <div class="control-group  {{ errors.password ? 'has-error' : '' }}">
            <div class="controls">
                <div class="main_input_box">
                    <span class="add-on bg_lg"><i class="icon-lock"> </i></span>
                    <input name="password" type="password"
                    value="" placeholder="Password baru" />
                    {% if errors.password %}
                    <span style="color:red;" class="help-inline">{{errors.password | first}}</span>
                    {% endif %}
                </div>
            </div>
        </div>
        <div class="control-group  {{ errors.password2 ? 'has-error' : '' }}">
            <div class="controls">
                <div class="main_input_box">
                    <span class="add-on bg_ly"><i class="icon-lock"></i></span>
                    <input type="hidden" name="token" value="{{ token }}">
                    <input name="password2" type="password"
                    value="" placeholder="Ulangi Password" />
                    {% if errors.password2 %}
                    <span style="color:red;" class="help-inline">{{errors.password2 | first}}</span>
                </div>
                {% endif %}
            </div>
        </div>
        <div class="form-actions">
            <span class="pull-right">
                <button type="submit" class="btn btn-success">
                    <i class="fa fa-login"></i>Reset Password</button></span>
        </div>
    </form>
</div>
<script src="{{ base_url() }}/js/jquery.min.js"></script>
<script src="{{ base_url() }}/js/matrix.login.js"></script>
</body>
</html>
